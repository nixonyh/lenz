[package]
name = "lenz"
description = "Type level field paths for a struct."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
keywords = ["macro", "field", "path", "lens", "reflection"]
categories = ["no-std", "rust-patterns"]

[dependencies]
lenz_macros = { workspace = true }

[lints]
workspace = true
